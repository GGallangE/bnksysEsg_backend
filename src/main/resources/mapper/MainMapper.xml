<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bnksys.esg.mapper.MainMapper">

    <select id="getUserWithBoards" resultType="userboard">
        SELECT u.id as user_id
             , u.name as user_name
             , b.id as board_id
             , b.name as board_name
          FROM User u
     LEFT JOIN Board b ON u.id = b.u_id
    </select>

    <select id = "getApiList" resultType="apiResult">
        SELECT *
          FROM API_LIST
         WHERE API_NAME LIKE CONCAT('%', #{name}, '%')
    <if test="sortBy == 'register' ">
      ORDER BY API_LIST_ID DESC
    </if>
    <if test="sortBy == 'view' ">
      ORDER BY VIEW DESC
    </if>
    <if test="sortBy == 'nbruses' ">
      ORDER BY NBR_USES DESC
    </if>
    </select>

</mapper>